apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: wasteco

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app.kubernetes.io/name: wasteco-api
  app.kubernetes.io/part-of: community-waste-collection
