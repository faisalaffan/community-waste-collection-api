apiVersion: v1
kind: Service
metadata:
  name: wasteco-api
  namespace: wasteco
spec:
  selector:
    app: wasteco-api
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP
