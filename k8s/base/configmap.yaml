apiVersion: v1
kind: ConfigMap
metadata:
  name: wasteco-config
  namespace: wasteco
data:
  APP_PORT: "8080"
  DB_HOST: "postgres-faisalaffan.singapura-parore.ts.net"
  DB_PORT: "5432"
  DB_NAME: "waste_collection"
  S3_ENDPOINT: "minio-s3-faisalaffan.singapura-parore.ts.net:9000"
  S3_BUCKET: "waste"
  S3_USE_SSL: "false"
